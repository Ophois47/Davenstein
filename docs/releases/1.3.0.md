# Davenstein 1.3.0

Davenstein 1.3.0 expands the project’s native release architecture onto 32-bit x86 Windows and Linux systems while completing a shared touchscreen experience for Android, iPhone, and iPad and adding a permanent native Apple mobile application host

The release adds native Windows i686 MSVC packaging, Linux i686 GNU cross-compilation, architecture-aware installation and portable packaging, object-format and ABI verification, state-aware visible mobile controls, complete touch-only navigation, native iPhone and iPad device and simulator builds, deterministic end-of-level tally sequencing, mobile full-screen and safe-area corrections, a first-shot input correction, Retina/HiDPI intermission layout repair, elimination of the magenta mobile startup flash, and a physically verified correction for the Android Application Not Responding condition

Davenstein remains a ground-up Wolfenstein 3-D reimplementation written in Rust with Bevy. The new packages, shared mobile interface, Apple mobile host, validation systems, and platform corrections described below extend the same Rust application rather than introducing separate gameplay implementations for individual platforms. Android remains the publicly packaged mobile target, while iPhone and iPad support is limited to private local development and unsigned continuous-integration compile proofs rather than App Store, TestFlight, or enterprise distribution

## Major Highlights

- Added native Windows i686 MSVC releases with both an NSIS installer and portable ZIP
- Added Linux i686 portable releases targeting little-endian 32-bit x86 systems
- Added target-specific i386 native dependency preparation and pre-build linker validation
- Added ELF32 Intel 80386, byte-order, loader, shared-library, checksum, package-content, and extracted-artifact validation
- Added PE32 `IMAGE_FILE_MACHINE_I386` validation for compiled, portable, and installed Windows executables
- Added imported DLL auditing for the Windows i686 executable
- Added architecture-aware NSIS installation through the 32-bit Program Files location and Registry32
- Added silent installation, Start Menu, installed-file, registry-view, and uninstall-cleanup validation
- Added state-aware visible touchscreen controls for gameplay, menus, tap-to-continue screens, and input-disabled states
- Added complete touch-only menu navigation with explicit confirmation and cancellation controls
- Added four-way floating D-pad movement, visible stick feedback, a held floating horizontal turn stick, action buttons, top-center weapon selection, and pause access
- Added independently adjustable touch turn sensitivity and movement deadzone settings with final mobile defaults of `0.6` and `0.40`
- Added a permanent native iPhone and iPad Xcode host with physical ARM64 device builds, Apple Silicon simulator builds, application metadata, a complete icon catalog, generated asset integration, and private local signing support
- Added unsigned iPhone and iPad device and simulator compile proofs with bundle, architecture, Mach-O platform, asset, metadata, checksum, and tagged-release gating validation
- Corrected mobile full-screen and safe-area behavior, exposed touchscreen controls from launch, deferred iOS edge gestures, and hid the home indicator
- Hid desktop-only Display, Resolution, VSync, and Quit rows on Android, iPhone, and iPad
- Corrected the first fire press after level start, respawn, and menu close while preserving held-confirmation suppression
- Corrected Retina/HiDPI end-of-level statistic spacing without disturbing status-bar projection
- Eliminated the magenta startup flash on mobile by adding a persistent lowest-order background camera that clears the window to black before the splash screen and normal rendering cameras initialize
- Added touch and gamepad handling for intermission, game over, cheat messages, and high-score name entry
- Corrected the end-of-level intermission regression by explicitly ordering tally initialization, counting, bonus application, confirmation handling, text synchronization, and overlay visibility
- Corrected the Android Application Not Responding condition and verified the result on physical hardware
- Added focused touch hit-testing, layout, accessibility, mode-selection, and overlay regression coverage
- Restored direct Unix execution of the committed Android Gradle wrapper

## Linux i686 Cross-Compilation

Davenstein now publishes a Linux i686 portable archive built through Rust’s `i686-unknown-linux-gnu` target

The build uses the maintained i686 cross image and enables Davenstein’s `software_render` feature so the package does not assume a proven target GPU driver. On Apple Silicon hosts, the same amd64 build environment can run through Docker or Colima by explicitly selecting the `linux/amd64` container platform

The Linux i686 workflow is independently selectable for development validation, publishes a dedicated `Davenstein-Linux-i686-Packages` artifact, and is required before a tagged release can be published

## Linux i386 Native Dependency Preparation

A dedicated `scripts/cross_prepare_i686.sh` preparation path installs and validates the 32-bit Linux development interfaces required by Bevy’s windowing, input, audio, and device layers

The target dependency set includes

- Wayland client development files
- ALSA development files
- libudev development files
- X11 development files
- Xcursor development files
- Xi development files
- xkbcommon development files
- Xrandr development files

The preparation path queries every required interface through i386-aware `pkg-config`, links a 32-bit target C probe, and inspects that probe before Cargo is allowed to compile Davenstein

This prevents a nominal i686 build from silently resolving host-architecture libraries or proceeding with incomplete target metadata

## Linux ELF32 Package Validation

The compiled Linux i686 executable is validated independently of the Rust target name

The workflow requires

- ELF32 class
- Little-endian byte order
- Intel 80386 machine identity
- Dynamic linkage through `/lib/ld-linux.so.2`
- Expected declarations for the required Wayland, libudev, ALSA, libgcc, libm, and glibc runtime libraries
- A non-empty executable with executable permissions

The generic Linux portable packager assembles the binary with `assets.pak`, the launcher, portable-storage marker, icon, README, licensing documents, copyright notice, and third-party asset documentation

The completed archive receives a SHA-256 checksum, is extracted into a clean directory, and is checked for its expected contents and executable modes. The extracted executable is then revalidated as little-endian Intel 80386 ELF32 with the same dynamic loader so the uploaded package is proven to retain the ABI established before packaging

## Windows i686 MSVC Build

Davenstein now builds a native 32-bit Windows executable through Rust’s `i686-pc-windows-msvc` target on a Windows Server 2022 release runner

The native MSVC path avoids treating the existing x86-64 GNU package as a substitute for Win32 support. It uses the Windows SDK and C++ x86 build tools available to the runner and produces `Davenstein.exe` directly for the 32-bit Windows ABI

The Windows i686 workflow is independently selectable, publishes a dedicated `Davenstein-Windows-i686-Packages` artifact, and is required before a tagged GitHub Release can be published

## Windows PE32 And Imported DLL Auditing

The original Windows i686 executable is inspected with LLVM COFF tooling immediately after compilation

The workflow requires `IMAGE_FILE_MACHINE_I386` and explicitly rejects AMD64 or ARM64 machine identities. Imported DLL declarations are captured and audited so the package’s runtime dependencies remain visible during release validation

The architecture check is repeated after the portable ZIP is extracted and again after the NSIS installer places the executable in its final installed location

This establishes the same Win32 machine identity across the compiler output, portable release, and installed application rather than trusting only the requested Rust target

## Windows i686 Portable Packaging

The portable Windows i686 ZIP contains

- `Davenstein.exe`
- `assets.pak`
- `README.md`
- Project licensing and copyright documents
- Third-party asset documentation
- `Davenstein.ico`
- `portable.flag`

The presence of `portable.flag` preserves Davenstein’s established portable-storage behavior so save games, high scores, and settings remain beside the extracted application rather than being redirected into an installed application-data location

The archive receives its own SHA-256 checksum, is expanded into a clean validation directory, and is rejected if required files are missing, `assets.pak` is empty, or the extracted executable is not PE32 i686

## Windows i686 NSIS Installation

The Windows installer was made architecture-aware without duplicating the complete NSIS definition

When built with the Win32 release definition, the installer

- Produces an i686-specific filename
- Uses the 32-bit Program Files location, which is `Program Files (x86)` on 64-bit Windows
- Writes application and uninstall metadata through the 32-bit registry view
- Preserves the existing Windows 95-era presentation
- Installs the executable, assets, icon, README, licensing documents, copyright notice, and third-party asset documentation
- Creates all-users Start Menu shortcuts
- Omits `portable.flag` from installed packages

The existing x86-64 installer retains its Program Files and Registry64 behavior when the Win32 definition is absent

## Win32 Registry And Uninstall Validation

The release workflow executes the completed installer silently on a clean native Windows runner

Validation requires

- A clean Program Files (x86) destination before installation
- No pre-existing Davenstein application or uninstall keys in Registry32 or Registry64
- Successful silent installation
- Every required installed file
- A PE32 i686 installed executable
- The expected Registry32 installation and uninstall records
- No corresponding records in Registry64
- The expected release version and installation path
- An all-users Start Menu directory

The installed uninstaller is then executed silently. The workflow waits for removal to complete and rejects the package if the installation directory, Start Menu directory, Registry32 records, or unexpected Registry64 records remain

This validates both installation and cleanup behavior using the actual package delivered to users

## State-Aware Touchscreen Architecture

Touch input is now governed by a shared `TouchUiMode` resource with four explicit modes

- `Gameplay` activates movement, turning, fire, use, weapon, and pause controls
- `Menu` activates a direction cluster with explicit confirm and back controls
- `Advance` converts a deliberate tap into confirmation on screens that only need to continue
- `Off` disables the glass during loading, death-fizzle, or touch-disabled states

The UI layer derives the active mode from the same splash, menu, game-over, intermission, death, cheat-message, and control-lock state used by the application itself

The input source and visible overlay read the same mode and the same `TouchLayout` geometry. A control therefore cannot be visible while inactive or tappable while absent

## Visible Touch Overlay

The touchscreen interface is rendered in window-space through the persistent menu UI camera rather than through Davenstein’s low-resolution gameplay canvas

Gameplay displays

- A floating movement control under the owning thumb
- A visible stick base and knob that reflect the actual clamped movement deflection
- A floating horizontal turn control with visible base and knob feedback
- FIRE and USE controls
- Four direct weapon-selection controls
- A MENU control

Menus display a bottom-left direction cluster and right-thumb OK and BACK controls. Confirm deliberately reuses FIRE’s rectangle and back reuses USE’s rectangle so the right thumb keeps the same resting action arc between gameplay and menus

Tap-to-continue screens display an explicit instruction rather than presenting unrelated gameplay buttons

The overlay follows `ActiveInputDevice`: it appears when touch is driving the application and hides when keyboard or gamepad input becomes active

## Four-Way Touch Movement

The floating movement control now behaves as a four-way D-pad rather than an analog stick

After the centre deadzone, the dominant axis wins and emits full cardinal movement

- Horizontal movement strafes left or right
- Vertical movement moves forward or backward
- Diagonal and partial-speed drift are not emitted
- Pushing into the outer ring activates running
- The initial touch position remains the floating origin

This behavior better matches Wolfenstein-style corridor movement while retaining a control that appears beneath the player’s thumb instead of requiring a fixed pad location

Eight-way diagonal movement remains a possible future enhancement rather than part of this release

## Touch Menu Navigation And Safe Confirmation

Touch now contributes through the same `MenuNav` vocabulary used by keyboard and gamepad input

The visible menu cluster maps to

- Up
- Down
- Left
- Right
- Confirm
- Cancel

Menu mode intentionally does not implement tap-anywhere confirmation. A tap must land on the explicit visible OK control before the highlighted item activates, preventing an arbitrary stray touch from selecting destructive actions such as Quit

Gameplay mode exposes only the pause button to the menu accumulator, while input-disabled mode ignores the glass entirely

## Touch-Driven Nonstandard Screens

Screens that exist only to advance now accept touch through `MenuNav.confirm`

This includes

- Initial splash screens
- Score presentation
- Episode victory and end text
- Mission-success intermission
- Game-over presentation
- The active cheat-message modal

The intermission and game-over paths read `MenuNav` after input gathering so they receive the current frame’s gamepad or touchscreen confirmation even while normal player control is locked

The death fizzle and loading states remain touch-disabled so a held or stray finger cannot skip internal transitions

## Touch And Gamepad High-Score Name Entry

High-score name entry no longer requires letter keys

The shared menu controls provide a three-character carousel

- Up and down cycle the current character through A-Z and space
- Right commits the current character and starts the next position
- Left or cancel deletes the current character
- Confirm submits the completed name

Keyboard letter entry remains available. W, A, S, and D keep their letter meaning on this screen instead of being double-consumed as navigation

A newly spawned name-entry screen ignores the same-frame confirmation that dismissed game over, preventing an empty name from being submitted before the player sees the interface

## Android ANR Correction And Physical Validation

Physical Android testing previously produced repeated system Application Not Responding dialogs even while Davenstein could continue rendering or playing

The consolidated Android input work corrected that condition. Subsequent testing on physical hardware verified that the warning no longer recurred while gameplay, rendering, touch controls, menu navigation, and dismissal behavior remained responsive

This release documents the observed and verified outcome without assigning an unsupported internal root cause beyond the completed Android changes

## Touch Geometry And Regression Validation

Touch rectangles are computed in real window pixels and scale within the established UI-scale limits rather than inheriting the 320x240 gameplay canvas

The layout includes accessibility-floor checks for every gameplay and menu target across representative phone, tablet, and desktop sizes

Automated coverage verifies

- Every menu button maps to the correct `MenuNav` bit
- Stray menu taps never confirm
- Gameplay maps only the pause rectangle into menu input
- Advance mode converts a deliberate tap into confirmation
- Off mode ignores every touch
- Gameplay buttons do not overlap one another
- Menu controls do not overlap one another
- Confirm and back retain the FIRE and USE action arc
- The movement region remains clear of the weapon row on narrower tablet layouts
- Weapon rectangles map to the correct one-based slots
- Unmeasured layouts claim no touches
- Touch-disabled states produce no overlay controls
- Screen-state priority selects the correct control mode

The test predicate table uses boxed `MenuNav` readers so the heterogeneous closure types compile as one explicit array. The Android Gradle wrapper is also committed executable for direct Unix invocation

The focused touch test set, complete Rust test suite, debug build, release build, and diff validation completed successfully after the follow-up correction

## Deterministic End-of-Level Intermission

The end-of-level tally is now scheduled as one explicit Bevy state machine instead of relying on incidental ordering among independent `Update` systems

The enforced sequence is

1. Tick the elevator-exit delay
2. Initialize the tally targets and displayed values
3. Advance the displayed kill, secret, treasure, time, par, and bonus values
4. Apply the completion bonus exactly once
5. Process current-frame confirmation input after `InputGather` and bonus finalization
6. Synchronize the displayed tally text
7. Synchronize the intermission overlay visibility

This prevents the text layer from observing pre-initialization values, prevents the ticker from running before its targets exist, and prevents level advancement from replacing `CurrentLevel` before the completed floor’s score and episode statistics are finalized

The correction does not change the percentage, time, par, or bonus calculations. It establishes deterministic ownership and ordering around the existing calculations

Repeated physical runtime testing no longer reproduced the tally glitch or inconsistent counting. The correction also passed all 47 Rust tests, the debug build, release build, asset-pack rebuild, and diff validation

## Release Workflow Integration

The Windows i686, Linux i686, and iPhone/iPad jobs are exposed through the manual release selector and publish target-specific workflow artifacts

The `ios-arm64` selector runs the permanent Xcode host on a macOS runner, installs the `aarch64-apple-ios` and `aarch64-apple-ios-sim` Rust targets, generates `assets.pak`, and validates the committed project, shared scheme, property list, scripts, and application icon catalog before compilation

The iPhone and iPad job builds an unsigned generic ARM64 physical-device application and an unsigned Apple Silicon simulator application. It verifies each bundle’s identifier, release version, iPhone and iPad device families, supported platform, executable architecture, Mach-O platform marker, asset pack, compiled asset catalog, and property-list validity

The completed device and simulator applications are archived separately with SHA-256 manifests and uploaded under `ios-iphone-ipad-build-validation`. That artifact name deliberately does not match the `Davenstein-*` public release pattern, keeping unsigned Apple mobile compile proofs separate from installable public packages

Tagged release publication now depends on successful completion of the Windows i686 package job, Linux i686 package job, and unsigned iPhone/iPad device and simulator compile proofs in addition to the previously supported targets

The Windows i686 portable and installer checksum manifests are written as explicit ASCII with an LF terminator. The workflow reads the completed files back as bytes and rejects any carriage return or missing final line feed, keeping the manifests directly consumable by Windows, Linux `sha256sum`, and macOS `shasum`

The validation model remains consistent with the rest of Davenstein’s release system

1. Compile for the requested target
2. Inspect the native object format, architecture, and platform identity
3. Build `assets.pak`
4. Assemble the platform package or isolated compile-proof application
5. Verify checksums, metadata, and required contents
6. Extract, install, or inspect the completed artifact as the format permits
7. Revalidate the executable and platform metadata
8. Exercise removal where the package format supports it
9. Upload only proven release artifacts or deliberately isolated compile proofs
10. Require the target validation before publishing a tagged release

## Implementation Record

- `1dbd758d61e39081f839653d12e13b8edd0509a7` added Linux i686 cross-compilation, validated i386 native dependencies, ELF32 checks, portable packaging, workflow selection, artifact publication, and tagged-release gating
- `3b3c13310f9569a812e8c00589766837f304acb8` added native Windows i686 MSVC packaging, PE32 checks, imported DLL auditing, architecture-aware NSIS installation, 32-bit registry validation, silent uninstall cleanup, artifact publication, and tagged-release gating
- `e4a0a9e015ab48a74be39e8ce1346db3830df3f6` added visible touchscreen controls, four-way movement, state-aware touch modes, complete menu navigation, nonstandard-screen confirmation, and touch/gamepad name entry
- `20b274b2a2e2d29b6793b5f54f259f229084376a` corrected touchscreen test typing, restored executable Android Gradle-wrapper permissions, and completed the focused and full regression validation
- `4144bdb8e2c35be5283bba463206442574213c3e` corrected the end-of-level intermission regression by ordering tally initialization, ticking, bonus application, confirmation handling, text synchronization, and overlay visibility as one deterministic state machine
- `91bc91370f22ac0bbda2ffffa310e0c91507caf6` corrected handheld windowing and safe-area behavior by preventing desktop resolution writes, enforcing borderless full-screen mode, self-healing persisted desktop display settings, hiding Display and Resolution controls, deferring iOS edge gestures, hiding the home indicator, and defaulting mobile input ownership to touch
- `285681e390d600d06838cec708d235837acc09a6` updated touchscreen overlay guidance and control labels to match the revised mobile interface
- `7333c6cd71adc1583f84c9b12b12119d0ebefc6f` hid the trailing Quit menu action on Android, iPhone, and iPad through platform-aware menu labels, item counts, and action dispatch
- `97cb17f52ca23273815e294cdb558266b2f18cc5` split touchscreen tuning into independent turn-sensitivity and movement-deadzone settings, exposed Move Deadzone in the Controls menu, persisted both values, and clamped them at the input read site
- `b7a93fbfef672fc50e3b83636225fd343e00cf74` enabled full-screen iOS launch behavior for notched devices and moved the touch weapon-selection row to the top center
- `8fadd45269a99bc4f0469b4f5f35c6b282815044` corrected Retina and HiDPI end-of-level statistic spacing by deriving the tally content scale from logical window width
- `1dafef653c4dbe18d8cf0d30c27f2a03127e7ee8` corrected the fire-release latch so a fresh first shot is accepted after level start, respawn, and menu close while held confirmation input remains suppressed
- `a25fb730e902f628b25a3bf59bde67d92058c20e` set the final shipping mobile defaults to touch turn sensitivity `0.6` and movement deadzone `0.40`
- `64101dfe2e1f5dc44305ab61ee2c4ea916c8b1d4` hid the ineffective VSync row from the mobile Change View menu
- `49b66704c00bee613e9ff2525f242e0546a7ee56` added the permanent iPhone and iPad Xcode host, reproducible Rust and asset build scripts, complete application metadata and icon catalog, unsigned physical-device and simulator CI compile proofs, generated-file exclusions, and Apple mobile build and private-distribution documentation
- `d21cd34c2477493204445b1cf694cbdfc09f86c9` eliminated the magenta startup flash on mobile by adding a persistent lowest-order background camera that clears the window to black before the splash screen and normal rendering cameras initialize

## Compatibility Notes

The Windows i686 packages target the 32-bit MSVC ABI. The installer uses the 32-bit Program Files location and Registry32, while the portable ZIP keeps mutable state beside the extracted application

The Linux i686 package targets little-endian Intel 80386 ELF32 systems and expects `/lib/ld-linux.so.2`. The target system must provide compatible 32-bit Wayland, libudev, ALSA, libgcc, libm, and glibc runtime libraries

The Linux i686 release uses software rendering by default. Performance depends on the target CPU, display stack, and available 32-bit system libraries

The Android public package remains ARM64 `arm64-v8a`. The touchscreen work is implemented in the shared Rust input and UI systems and is therefore not an Android-specific duplicate of gameplay logic

Touch movement is four-way in this release. Eight-way diagonal movement is not yet implemented

Davenstein 1.3.0 includes a permanent native iPhone and iPad Xcode host for private physical-device development and Apple Silicon simulator validation. The continuous-integration workflow produces unsigned device and simulator compile proofs, but App Store, TestFlight, enterprise, and public iOS package distribution are not supported

## Full Comparison

| Area | Davenstein 1.2.0 | Davenstein 1.3.0 |
| --- | --- | --- |
| Windows x86 support | x86-64 installer and portable ZIP | Adds native i686 MSVC installer and portable ZIP |
| Linux x86 support | x86-64 packages | Adds little-endian Intel 80386 ELF32 portable archive |
| Windows installer architecture | x86-64 Program Files and registry behavior | Adds 32-bit Program Files selection, Registry32, Win32 filename, install, and uninstall validation |
| Windows executable validation | PE32+ x86-64 and ARM64 machine checks | Adds PE32 i686 checks before packaging, after extraction, and after installation |
| Linux dependency preparation | Target-specific preparation for existing cross targets | Adds validated i386 `pkg-config` interfaces and a linked ELF32 target probe |
| Linux package validation | Existing ELF64 target checks | Adds ELF32, byte order, Intel 80386, loader, shared-library, archive, and extracted-artifact checks |
| Mobile gameplay touch | Touch hit regions without a complete visible interface | Adds shared Android and iPhone/iPad controls with visible floating movement and held horizontal turn sticks, FIRE, USE, weapon selection, and MENU |
| Mobile movement | Analog-style floating movement | Adds dominant-axis four-way movement with independently adjustable deadzone, defaulting to `0.40` |
| Mobile turning | Repeated horizontal drag movement | Adds a floating horizontal stick with a held frame-rate-independent turn rate and default sensitivity `0.6` |
| Mobile menus | Touch could open pause but not complete every flow | Adds direction, OK, BACK, safe explicit confirmation, complete touch-only navigation, and platform-aware removal of desktop-only rows |
| Mobile display options | Desktop Display, Resolution, and VSync rows remained visible | Hides Display, Resolution, and VSync on handhelds and enforces borderless full-screen behavior |
| Mobile application exit | Quit remained visible despite terminating the handheld application | Hides the trailing Quit row on Android, iPhone, and iPad |
| Mobile modal screens | Primarily keyboard or gamepad advancement | Adds tap-to-continue for splash, score, victory, game over, intermission, and cheat-message screens |
| High-score entry | Keyboard letters and Enter | Adds gamepad and touchscreen character carousel, deletion, positioning, and confirmation |
| Touch-control placement | Weapon controls occupied the lower movement area | Moves weapon selection to the top center and keeps the lower edges available for thumb controls |
| Mobile control visibility | Touch regions could remain invisible at launch | Defaults handheld input ownership to touch and displays the overlay from application startup |
| iPhone and iPad host | No committed native Apple mobile application host | Adds a permanent Xcode project, shared scheme, application metadata, icon catalog, Rust build phase, and generated asset integration |
| iPhone and iPad builds | No permanent device or simulator build path | Adds ARM64 physical-device and Apple Silicon simulator builds with private local signing support |
| iPhone and iPad CI | No Apple mobile compile proof | Adds unsigned device and simulator builds with bundle, architecture, platform, asset, checksum, and metadata validation |
| iPhone full-screen behavior | Legacy compatibility sizing could letterbox notched devices | Adds an explicit launch-screen declaration, full-screen layout, deferred edge gestures, and hidden home indicator |
| Mobile safe-area handling | Desktop resolution writes could desynchronize rendering and touch coordinates | Prevents handheld resolution writes, forces borderless mode, and self-heals persisted desktop display settings |
| Mobile startup rendering | The pre-splash window could expose an uninitialized magenta swapchain drawable | Adds a persistent lowest-order black-clearing background camera until the splash and gameplay cameras take over |
| First fire press | A release latch could consume the first shot after level start, respawn, or menu close | Allows a fresh fire edge through while still suppressing a carried held confirmation |
| Retina intermission layout | HiDPI scaling could compress end-of-level stat lines | Derives tally content scale from logical window width while retaining projected status-bar placement |
| Android responsiveness | Physical testing exposed repeated Application Not Responding dialogs | Corrected and verified on physical Android hardware |
| Physical mobile validation | Android testing was incomplete and no permanent iPhone host existed | Verifies Android gameplay and responsiveness plus signed installation and gameplay on a physical iPhone |
| Touch regression coverage | Limited touch geometry coverage | Adds mode, hit-test, overlap, accessibility-floor, turn-axis, overlay, and screen-priority tests |
| End-of-level intermission | Tally systems could depend on incidental scheduler order | Adds deterministic initialization, counting, bonus, input, text, and overlay sequencing |
| Tagged release gating | Required the previously supported platform jobs | Adds Windows i686 and Linux i686 packages plus unsigned iPhone/iPad device and simulator compile proofs |
